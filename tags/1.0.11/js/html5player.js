function init() {var video = document.getElementById("Html5Player");if (video.canPlayType) {document.getElementById("buttonbar").style.display = "block";document.getElementById("inputField").style.display = "block";function vidplay(evt) {if (video.src == "") {getVideo();}button = evt.target;if (video.paused) {video.play();button.textContent = "||";} else {video.pause();button.textContent = ">";}}}}